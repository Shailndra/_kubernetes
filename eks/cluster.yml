apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: demo-cluster
  region: us-east-1

nodeGroups:
  - name: demo-ng
    instanceType: t3a.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 4
#   ssh: 
#     allow: true
#     publicKeyName: demossh-key




